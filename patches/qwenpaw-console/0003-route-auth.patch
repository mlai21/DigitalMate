diff --git a/src/App.tsx b/src/App.tsx
index 53fb39a..3439cb8 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -8 +8 @@ import { App as AntdApp } from "antd";
-import { BrowserRouter, Routes, Route, Navigate } from "react-router-dom";
+import { BrowserRouter, Routes, Route } from "react-router-dom";
@@ -31,5 +30,0 @@ import { UpdateTakeoverGate } from "./components/UpdateTakeoverPage";
-import { Suspense } from "react";
-import { lazyImportWithRetry } from "./utils/lazyWithRetry";
-
-const LoginPage = lazyImportWithRetry("./pages/Login/index");
-import { authApi } from "./api/modules/auth";
@@ -38 +32,0 @@ import { useUploadLimitStore } from "./stores/uploadLimitStore";
-import { getApiUrl, getApiToken, clearAuthToken } from "./api/config";
@@ -76,27 +70,6 @@ function AuthGuard({ children }: { children: React.ReactNode }) {
-        const res = await authApi.getStatus();
-        if (cancelled) return;
-        if (!res.enabled) {
-          setStatus("ok");
-          return;
-        }
-        const token = getApiToken();
-        if (!token) {
-          setStatus("auth-required");
-          return;
-        }
-        try {
-          const r = await fetch(getApiUrl("/auth/verify"), {
-            headers: { Authorization: `Bearer ${token}` },
-          });
-          if (cancelled) return;
-          if (r.ok) {
-            setStatus("ok");
-          } else {
-            clearAuthToken();
-            setStatus("auth-required");
-          }
-        } catch {
-          if (!cancelled) {
-            clearAuthToken();
-            setStatus("auth-required");
-          }
+        const response = await fetch("/api/admin/compat/auth/status", {
+          credentials: "same-origin",
+          headers: { Accept: "application/json" },
+        });
+        if (!response.ok) {
+          throw new Error("Failed to check DigitalMate auth status");
@@ -103,0 +77,5 @@ function AuthGuard({ children }: { children: React.ReactNode }) {
+        const result = (await response.json()) as {
+          authenticated?: boolean;
+        };
+        if (cancelled) return;
+        setStatus(result.authenticated === true ? "ok" : "auth-required");
@@ -105 +83 @@ function AuthGuard({ children }: { children: React.ReactNode }) {
-        if (!cancelled) setStatus("ok");
+        if (!cancelled) setStatus("auth-required");
@@ -114,7 +92 @@ function AuthGuard({ children }: { children: React.ReactNode }) {
-  if (status === "auth-required")
-    return (
-      <Navigate
-        to={`/login?redirect=${encodeURIComponent(window.location.pathname)}`}
-        replace
-      />
-    );
+  if (status === "auth-required") return <DigitalMateLoginRedirect />;
@@ -123,0 +96,8 @@ function AuthGuard({ children }: { children: React.ReactNode }) {
+function DigitalMateLoginRedirect() {
+  useEffect(() => {
+    const returnTo = `${window.location.pathname}${window.location.search}`;
+    window.location.assign(`/login?redirect=${encodeURIComponent(returnTo)}`);
+  }, []);
+  return null;
+}
+
@@ -125 +105,10 @@ function getRouterBasename(pathname: string): string | undefined {
-  return /^\/console(?:\/|$)/.test(pathname) ? "/console" : undefined;
+  if (
+    pathname === "/admin-preview" ||
+    pathname.startsWith("/admin-preview/")
+  ) {
+    return "/admin-preview";
+  }
+  if (pathname === "/admin" || pathname.startsWith("/admin/")) {
+    return "/admin";
+  }
+  return undefined;
@@ -214,5 +203 @@ function AppInner() {
-                    element={
-                      <Suspense fallback={null}>
-                        <LoginPage />
-                      </Suspense>
-                    }
+                    element={<DigitalMateLoginRedirect />}
diff --git a/src/layouts/registry/builtinRoutes.tsx b/src/layouts/registry/builtinRoutes.tsx
index 48d64ec..e8f9fd1 100644
--- a/src/layouts/registry/builtinRoutes.tsx
+++ b/src/layouts/registry/builtinRoutes.tsx
@@ -15 +15 @@
-import { Suspense } from "react";
+import { Suspense, useEffect } from "react";
@@ -17,2 +16,0 @@ import { Navigate } from "react-router-dom";
-import { Spin } from "antd";
-import { useTranslation } from "react-i18next";
@@ -20 +17,0 @@ import { lazyImportWithRetry } from "../../utils/lazyWithRetry";
-import { useCodingMode } from "../../stores/codingModeStore";
@@ -25 +21,0 @@ import type { Route } from "../../plugins/registry/types";
-import Chat from "../../pages/Chat";
@@ -59,2 +55,2 @@ const PluginManagerPage = lazyImportWithRetry(
- * "/" lands here. Waits for useSyncCodingMode to populate the store before
- * deciding between /coding and /chat — see MainLayout.tsx history for why.
+ * "/" lands on the first Console management page. Chat remains on the
+ * DigitalMate home page instead of being duplicated inside the Console.
@@ -63,11 +59,8 @@ function DefaultRedirect() {
-  const { t } = useTranslation();
-  const { codingMode, initialized } = useCodingMode();
-  if (!initialized) {
-    return (
-      <Spin
-        tip={t("common.loading")}
-        style={{ display: "block", margin: "20vh auto" }}
-      />
-    );
-  }
-  return <Navigate to={codingMode ? "/coding" : "/chat"} replace />;
+  return <Navigate to="/inbox" replace />;
+}
+
+function DigitalMateHomeRedirect() {
+  useEffect(() => {
+    window.location.assign("/");
+  }, []);
+  return null;
@@ -83 +76 @@ export const BUILTIN_ROUTES: Route[] = [
-  { id: "core.chat", path: "/chat/*", component: Chat },
+  { id: "core.chat", path: "/chat/*", component: DigitalMateHomeRedirect },
