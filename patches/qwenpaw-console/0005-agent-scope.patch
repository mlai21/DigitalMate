diff --git a/src/api/agentScope.ts b/src/api/agentScope.ts
index 029aec5..e25290a 100644
--- a/src/api/agentScope.ts
+++ b/src/api/agentScope.ts
@@ -1,20 +1,35 @@
 const CANONICAL_UUID =
   /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/;

+let validatedAgentIds: ReadonlySet<string> = new Set<string>();
 let validatedDefaultAgentId: string | null = null;

 export function isCanonicalAgentId(value: unknown): value is string {
   return typeof value === "string" && CANONICAL_UUID.test(value);
 }

-export function markValidatedDefaultAgent(agentId: string): void {
-  validatedDefaultAgentId = isCanonicalAgentId(agentId) ? agentId : null;
+/** Records the agent ids the server listed for this account. */
+export function markValidatedAgents(
+  agentIds: readonly string[],
+  defaultAgentId: string | null,
+): void {
+  validatedAgentIds = new Set(agentIds.filter(isCanonicalAgentId));
+  validatedDefaultAgentId =
+    isCanonicalAgentId(defaultAgentId) &&
+    validatedAgentIds.has(defaultAgentId)
+      ? defaultAgentId
+      : null;
 }

-export function clearValidatedDefaultAgent(): void {
+export function clearValidatedAgents(): void {
+  validatedAgentIds = new Set<string>();
   validatedDefaultAgentId = null;
 }

+export function isValidatedAgent(agentId: unknown): agentId is string {
+  return isCanonicalAgentId(agentId) && validatedAgentIds.has(agentId);
+}
+
 export function getValidatedDefaultAgent(): string | null {
   return validatedDefaultAgentId;
 }
diff --git a/src/api/authHeaders.test.ts b/src/api/authHeaders.test.ts
index a877f42..465de7d 100644
--- a/src/api/authHeaders.test.ts
+++ b/src/api/authHeaders.test.ts
@@ -1,18 +1,16 @@
 import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
 import { buildAuthHeaders, buildMutationHeaders } from "./authHeaders";
 import { setCsrfToken } from "./config";
-import {
-  clearValidatedDefaultAgent,
-  markValidatedDefaultAgent,
-} from "./agentScope";
+import { clearValidatedAgents, markValidatedAgents } from "./agentScope";

 const DEFAULT_AGENT_ID = "10000000-0000-4000-8000-000000000011";
+const SECONDARY_AGENT_ID = "10000000-0000-4000-8000-000000000012";

 describe("auth headers", () => {
   beforeEach(() => {
     sessionStorage.clear();
     localStorage.clear();
-    clearValidatedDefaultAgent();
+    clearValidatedAgents();
     sessionStorage.setItem(
       "qwenpaw-agent-storage",
       JSON.stringify({ state: { selectedAgent: "default" } }),
@@ -33,20 +31,47 @@ describe("auth headers", () => {
   });

   it("sends the real selected UUID through both compatibility headers", () => {
-    markValidatedDefaultAgent(DEFAULT_AGENT_ID);
     sessionStorage.setItem(
       "qwenpaw-agent-storage",
       JSON.stringify({
         state: { selectedAgent: DEFAULT_AGENT_ID },
       }),
     );
-    markValidatedDefaultAgent(DEFAULT_AGENT_ID);
+    markValidatedAgents([DEFAULT_AGENT_ID], DEFAULT_AGENT_ID);
     expect(buildAuthHeaders()).toEqual({
       "X-Agent-Id": DEFAULT_AGENT_ID,
       "x-digitalmate-agent-id": DEFAULT_AGENT_ID,
     });
   });

+  it("scopes requests to a listed non-default agent", () => {
+    sessionStorage.setItem(
+      "qwenpaw-agent-storage",
+      JSON.stringify({
+        state: { selectedAgent: SECONDARY_AGENT_ID },
+      }),
+    );
+    markValidatedAgents(
+      [DEFAULT_AGENT_ID, SECONDARY_AGENT_ID],
+      DEFAULT_AGENT_ID,
+    );
+    expect(buildAuthHeaders()).toEqual({
+      "X-Agent-Id": SECONDARY_AGENT_ID,
+      "x-digitalmate-agent-id": SECONDARY_AGENT_ID,
+    });
+  });
+
+  it("drops a selection that the agent list never returned", () => {
+    sessionStorage.setItem(
+      "qwenpaw-agent-storage",
+      JSON.stringify({
+        state: { selectedAgent: SECONDARY_AGENT_ID },
+      }),
+    );
+    markValidatedAgents([DEFAULT_AGENT_ID], DEFAULT_AGENT_ID);
+    expect(buildAuthHeaders()).toEqual({});
+  });
+
   it.each(["GET", "HEAD", "OPTIONS"])(
     "does not attach CSRF to %s",
     async (method) => {
@@ -74,7 +99,7 @@ describe("auth headers", () => {
         state: { selectedAgent: DEFAULT_AGENT_ID },
       }),
     );
-    markValidatedDefaultAgent(DEFAULT_AGENT_ID);
+    markValidatedAgents([DEFAULT_AGENT_ID], DEFAULT_AGENT_ID);
     global.fetch = vi.fn().mockResolvedValue(
       Response.json({
         authenticated: true,
diff --git a/src/api/authHeaders.ts b/src/api/authHeaders.ts
index 6ee9d49..5e3f81c 100644
--- a/src/api/authHeaders.ts
+++ b/src/api/authHeaders.ts
@@ -4,10 +4,7 @@ import {
   hasFreshCsrfToken,
   setCsrfToken,
 } from "./config";
-import {
-  getValidatedDefaultAgent,
-  isCanonicalAgentId,
-} from "./agentScope";
+import { isValidatedAgent } from "./agentScope";

 const MUTATION_METHODS = new Set(["POST", "PUT", "PATCH", "DELETE"]);
 const CSRF_REFRESH_TIMEOUT_MS = 10_000;
@@ -24,10 +21,7 @@ export function buildAuthHeaders(): Record<string, string> {
     if (agentStorage) {
       const parsed = JSON.parse(agentStorage);
       const selectedAgent = parsed?.state?.selectedAgent;
-      if (
-        isCanonicalAgentId(selectedAgent) &&
-        selectedAgent === getValidatedDefaultAgent()
-      ) {
+      if (isValidatedAgent(selectedAgent)) {
         headers["X-Agent-Id"] = selectedAgent;
         headers["x-digitalmate-agent-id"] = selectedAgent;
       }
diff --git a/src/api/types/agents.ts b/src/api/types/agents.ts
index 56e9df5..1f39ad5 100644
--- a/src/api/types/agents.ts
+++ b/src/api/types/agents.ts
@@ -52,10 +52,25 @@ export interface AgentCapabilities {
   import: boolean;
   clone: boolean;
   delete: boolean;
+  toggle: boolean;
+  pin: boolean;
+  reorder: boolean;
 }

+export const DISABLED_AGENT_CAPABILITIES: AgentCapabilities = {
+  multi_agent: false,
+  create: false,
+  import: false,
+  clone: false,
+  delete: false,
+  toggle: false,
+  pin: false,
+  reorder: false,
+};
+
 export interface AgentListResponse {
   agents: AgentSummary[];
+  capabilities?: AgentCapabilities;
 }

 export interface ReorderAgentsResponse {
diff --git a/src/components/AgentSelector/AgentSelector.test.tsx b/src/components/AgentSelector/AgentSelector.test.tsx
index d6615d0..979b1e7 100644
--- a/src/components/AgentSelector/AgentSelector.test.tsx
+++ b/src/components/AgentSelector/AgentSelector.test.tsx
@@ -14,6 +14,16 @@ const mocks = vi.hoisted(() => ({
   storeState: {
     selectedAgent: "default",
     agents: [] as Array<Record<string, unknown>>,
+    capabilities: {
+      multi_agent: true,
+      create: false,
+      import: false,
+      clone: false,
+      delete: false,
+      toggle: false,
+      pin: false,
+      reorder: false,
+    },
   },
 }));

@@ -104,27 +114,41 @@ describe("AgentSelector", () => {
     expect(screen.queryByRole("combobox")).not.toBeInTheDocument();
   });

-  it("does not show secondary agent", async () => {
+  it("lists a non-default agent and switches scope to it", async () => {
     const user = userEvent.setup();
     renderWithProviders(<AgentSelector />);

     await user.click(screen.getByRole("combobox"));
     expect(screen.getAllByText("DigitalMate").length).toBeGreaterThan(0);
-    expect(screen.queryByText("Agent One")).not.toBeInTheDocument();
-    expect(screen.queryByText("Agent Two")).not.toBeInTheDocument();
+    expect(screen.getByText("Agent One")).toBeInTheDocument();
+
+    await user.click(screen.getByText("Agent One"));
+    expect(mocks.setSelectedAgent).toHaveBeenCalledWith("agent-1");
     expect(mocks.toggleAgentEnabled).not.toHaveBeenCalled();
     expect(mocks.setAgentPinned).not.toHaveBeenCalled();
   });

-  it("resets a previously selected secondary agent to default", async () => {
+  it("hides inline enable and disable controls while the toggle capability is off", async () => {
+    const user = userEvent.setup();
+    renderWithProviders(<AgentSelector />);
+
+    await user.click(screen.getByRole("combobox"));
+    expect(
+      screen.queryByLabelText("agent.enableAgent"),
+    ).not.toBeInTheDocument();
+    expect(
+      screen.queryByLabelText("agent.disableAgent"),
+    ).not.toBeInTheDocument();
+  });
+
+  it("keeps a listed non-default selection instead of forcing the default", async () => {
     mocks.storeState.selectedAgent = "agent-1";
     renderWithProviders(<AgentSelector />);

     await waitFor(() => {
-      expect(mocks.setSelectedAgent).toHaveBeenCalledWith(
-        "10000000-0000-4000-8000-000000000011",
-      );
+      expect(mocks.refreshAgents).toHaveBeenCalledOnce();
     });
+    expect(mocks.setSelectedAgent).not.toHaveBeenCalled();
     expect(mocks.toggleAgentEnabled).not.toHaveBeenCalled();
     expect(mocks.setAgentPinned).not.toHaveBeenCalled();
   });
diff --git a/src/components/AgentSelector/index.tsx b/src/components/AgentSelector/index.tsx
index b8b1dbd..5e745fa 100644
--- a/src/components/AgentSelector/index.tsx
+++ b/src/components/AgentSelector/index.tsx
@@ -32,8 +32,14 @@ export default function AgentSelector({
 }: AgentSelectorProps) {
   const { t } = useTranslation();
   const navigate = useNavigate();
-  const { selectedAgent, agents, setSelectedAgent, setAgents, refreshAgents } =
-    useAgentStore();
+  const {
+    selectedAgent,
+    agents,
+    capabilities,
+    setSelectedAgent,
+    setAgents,
+    refreshAgents,
+  } = useAgentStore();
   const { message } = useAppMessage();
   const messageRef = useRef(message);
   const translationRef = useRef(t);
@@ -70,10 +76,7 @@ export default function AgentSelector({
     void loadAgents();
   }, [loadAgents]);

-  const visibleAgents = useMemo(
-    () => agents.filter((agent) => agent.is_default === true),
-    [agents],
-  );
+  const visibleAgents = useMemo(() => agents, [agents]);
   const enabledAgents = useMemo(
     () => visibleAgents.filter((agent) => agent.enabled),
     [visibleAgents],
@@ -107,13 +110,6 @@ export default function AgentSelector({
     message.success(t("agent.switchSuccess"));
   };

-  useEffect(() => {
-    const defaultAgent = agents.find((agent) => agent.is_default === true);
-    if (!defaultAgent || selectedAgent === defaultAgent.id) return;
-
-    setSelectedAgent(defaultAgent.id);
-  }, [agents, message, selectedAgent, setSelectedAgent, t]);
-
   const stopSelectorAction = (event: MouseEvent<HTMLElement>) => {
     event.preventDefault();
     event.stopPropagation();
@@ -163,7 +159,12 @@ export default function AgentSelector({

   const handlePinAgent = useCallback(
     async (agent: AgentSummary) => {
-      if (agent.is_default === true || pinningAgentId !== null) return;
+      if (
+        !capabilities.pin ||
+        agent.is_default === true ||
+        pinningAgentId !== null
+      )
+        return;
       const nextPinned = !agent.pinned;
       setPinningAgentId(agent.id);
       setAgents(
@@ -186,7 +187,15 @@ export default function AgentSelector({
         setPinningAgentId(null);
       }
     },
-    [agents, loadAgents, message, pinningAgentId, setAgents, t],
+    [
+      agents,
+      capabilities.pin,
+      loadAgents,
+      message,
+      pinningAgentId,
+      setAgents,
+      t,
+    ],
   );
   const { getLongPressProps, pressingId, feedbackId } = useAgentLongPress(
     (agent) => {
@@ -324,7 +333,9 @@ export default function AgentSelector({
             </div>
           )}
         </div>
-        {agent.is_default !== true && renderToggleButton(agent, !agent.enabled)}
+        {agent.is_default !== true &&
+          capabilities.toggle &&
+          renderToggleButton(agent, !agent.enabled)}
       </div>
       <div className={styles.agentOptionId}>{`ID: ${agent.id}`}</div>
     </div>
diff --git a/src/pages/Settings/Agents/components/AgentTable.test.tsx b/src/pages/Settings/Agents/components/AgentTable.test.tsx
index df062a4..f6d9485 100644
--- a/src/pages/Settings/Agents/components/AgentTable.test.tsx
+++ b/src/pages/Settings/Agents/components/AgentTable.test.tsx
@@ -1,7 +1,11 @@
 import { act, screen, within } from "@testing-library/react";
 import userEvent from "@testing-library/user-event";
 import { describe, expect, it, vi } from "vitest";
-import type { AgentSummary } from "@/api/types/agents";
+import {
+  DISABLED_AGENT_CAPABILITIES,
+  type AgentCapabilities,
+  type AgentSummary,
+} from "@/api/types/agents";
 import { renderWithProviders } from "@/test/common_setup";
 import { AgentTable } from "./AgentTable";

@@ -37,6 +41,13 @@ const agent = (id: string, pinned: boolean): AgentSummary => ({
   startup_status: "running",
 });
 const DEFAULT_AGENT_ID = "10000000-0000-4000-8000-000000000011";
+const capabilities = (
+  overrides: Partial<AgentCapabilities> = {},
+): AgentCapabilities => ({
+  ...DISABLED_AGENT_CAPABILITIES,
+  multi_agent: true,
+  ...overrides,
+});

 describe("AgentTable", { timeout: 15_000 }, () => {
   it("uses click-specific labels for pin actions", () => {
@@ -50,6 +61,7 @@ describe("AgentTable", { timeout: 15_000 }, () => {
         onToggle={vi.fn()}
         onPin={vi.fn()}
         onReorder={vi.fn()}
+        capabilities={capabilities({ pin: true })}
       />,
     );

@@ -84,18 +96,20 @@ describe("AgentTable", { timeout: 15_000 }, () => {
         onToggle={onToggle}
         onPin={onPin}
         onReorder={vi.fn()}
-        secondaryAgentActionsDisabled
+        capabilities={capabilities()}
       />,
     );

     expect(screen.getByTitle("agent.defaultNotDeletable")).toBeDisabled();
     const secondaryRow = screen.getAllByText("secondary")[0].closest("tr");
     expect(secondaryRow).not.toBeNull();
-    const secondaryButtons = within(secondaryRow as HTMLElement).getAllByRole(
-      "button",
-    );
-    expect(secondaryButtons.length).toBeGreaterThanOrEqual(5);
-    for (const button of secondaryButtons) {
+    const gatedButtons = within(secondaryRow as HTMLElement)
+      .getAllByRole("button")
+      .filter(
+        (button) => button.getAttribute("aria-label") !== "agent.edit",
+      );
+    expect(gatedButtons.length).toBeGreaterThanOrEqual(4);
+    for (const button of gatedButtons) {
       expect(
         button.hasAttribute("disabled") ||
           button.getAttribute("aria-disabled") === "true",
@@ -108,6 +122,45 @@ describe("AgentTable", { timeout: 15_000 }, () => {
     expect(onPin).not.toHaveBeenCalled();
   });

+  it("disables capability-gated actions but keeps editing available", async () => {
+    const user = userEvent.setup();
+    const onEdit = vi.fn();
+    const secondaryAgent = {
+      ...agent("secondary", false),
+      name: "Alvin",
+      is_default: false,
+    };
+    renderWithProviders(
+      <AgentTable
+        agents={[secondaryAgent]}
+        loading={false}
+        reordering={false}
+        onEdit={onEdit}
+        onDelete={vi.fn()}
+        onToggle={vi.fn()}
+        onPin={vi.fn()}
+        onReorder={vi.fn()}
+        capabilities={capabilities()}
+      />,
+    );
+
+    const row = screen.getAllByText("Alvin")[0].closest("tr");
+    expect(row).not.toBeNull();
+    const edit = within(row as HTMLElement).getByRole("button", {
+      name: "agent.edit",
+    });
+    expect(edit).not.toBeDisabled();
+    await user.click(edit);
+    expect(onEdit).toHaveBeenCalledWith(secondaryAgent);
+    const gated = within(row as HTMLElement).getAllByTitle(
+      "agent.secondaryAgentUnsupported",
+    );
+    expect(gated.length).toBeGreaterThanOrEqual(2);
+    for (const button of gated) {
+      expect(button).toBeDisabled();
+    }
+  });
+
   it("does not reorder secondary agents", () => {
     const onReorder = vi.fn();
     renderWithProviders(
@@ -124,7 +177,7 @@ describe("AgentTable", { timeout: 15_000 }, () => {
         onToggle={vi.fn()}
         onPin={vi.fn()}
         onReorder={onReorder}
-        secondaryAgentActionsDisabled
+        capabilities={capabilities()}
       />,
     );

@@ -155,7 +208,7 @@ describe("AgentTable", { timeout: 15_000 }, () => {
         onToggle={vi.fn()}
         onPin={vi.fn()}
         onReorder={vi.fn()}
-        secondaryAgentActionsDisabled
+        capabilities={capabilities()}
       />,
     );

diff --git a/src/pages/Settings/Agents/components/AgentTable.tsx b/src/pages/Settings/Agents/components/AgentTable.tsx
index 64f6b69..3314f1b 100644
--- a/src/pages/Settings/Agents/components/AgentTable.tsx
+++ b/src/pages/Settings/Agents/components/AgentTable.tsx
@@ -15,7 +15,10 @@ import {
 } from "@dnd-kit/sortable";
 import { EditOutlined, DeleteOutlined, RobotOutlined } from "@ant-design/icons";
 import { EyeOff, Eye, Pin, PinOff } from "lucide-react";
-import type { AgentSummary } from "../../../../api/types/agents";
+import type {
+  AgentCapabilities,
+  AgentSummary,
+} from "../../../../api/types/agents";
 import { useTheme } from "../../../../contexts/ThemeContext";
 import { getAgentDisplayName } from "../../../../utils/agentDisplayName";
 import { SortableAgentRow, DragHandle } from "./SortableAgentRow";
@@ -32,7 +35,7 @@ interface AgentTableProps {
   onToggle: (agentId: string, currentEnabled: boolean) => void;
   onPin: (agentId: string, currentPinned: boolean) => void;
   onReorder: (activeId: string, overId: string) => void;
-  secondaryAgentActionsDisabled?: boolean;
+  capabilities: AgentCapabilities;
 }

 function isDefaultAgent(agent: AgentSummary): boolean {
@@ -48,7 +51,7 @@ export function AgentTable({
   onToggle,
   onPin,
   onReorder,
-  secondaryAgentActionsDisabled = false,
+  capabilities,
 }: AgentTableProps) {
   const { t } = useTranslation();
   const { isDark } = useTheme();
@@ -69,7 +72,7 @@ export function AgentTable({
     : {};

   const handleDragEnd = (event: DragEndEvent) => {
-    if (secondaryAgentActionsDisabled) {
+    if (!capabilities.reorder) {
       return;
     }
     const { active, over } = event;
@@ -94,7 +97,7 @@ export function AgentTable({
                 reordering ||
                 loading ||
                 isDefaultAgent(record) ||
-                secondaryAgentActionsDisabled
+                !capabilities.reorder
               }
             />
           </span>
@@ -176,21 +179,22 @@ export function AgentTable({
         const startupInProgress =
           record.startup_status === "pending" ||
           record.startup_status === "starting";
-        const secondaryActionsDisabled =
-          secondaryAgentActionsDisabled && !isDefaultAgent(record);
         const toggleDisabled =
           isDefaultAgent(record) ||
           startupInProgress ||
-          secondaryActionsDisabled;
-        const deleteDisabled = toggleDisabled;
-        const pinActionLabel =
-          isDefaultAgent(record)
-            ? t("agent.defaultPinned")
-            : secondaryActionsDisabled
-            ? t("agent.secondaryAgentUnsupported")
-            : record.pinned
-            ? t("agent.unpinAgent")
-            : t("agent.pinAgent");
+          !capabilities.toggle;
+        const deleteDisabled =
+          isDefaultAgent(record) ||
+          startupInProgress ||
+          !capabilities.delete;
+        const pinDisabled = isDefaultAgent(record) || !capabilities.pin;
+        const pinActionLabel = isDefaultAgent(record)
+          ? t("agent.defaultPinned")
+          : !capabilities.pin
+          ? t("agent.secondaryAgentUnsupported")
+          : record.pinned
+          ? t("agent.unpinAgent")
+          : t("agent.pinAgent");

         return (
           <Space>
@@ -207,12 +211,8 @@ export function AgentTable({
                   )
                 }
                 onClick={() => onPin(record.id, Boolean(record.pinned))}
-                disabled={isDefaultAgent(record) || secondaryActionsDisabled}
-                style={
-                  isDefaultAgent(record) || secondaryActionsDisabled
-                    ? disabledStyle
-                    : iconStyle
-                }
+                disabled={pinDisabled}
+                style={pinDisabled ? disabledStyle : iconStyle}
               />
             </Tooltip>
             <Button
@@ -221,17 +221,7 @@ export function AgentTable({
               icon={<EditOutlined />}
               aria-label={t("agent.edit")}
               onClick={() => onEdit(record)}
-              disabled={secondaryActionsDisabled}
-              style={
-                secondaryActionsDisabled
-                  ? disabledStyle
-                  : iconStyle
-              }
-              title={
-                secondaryActionsDisabled
-                  ? t("agent.secondaryAgentUnsupported")
-                  : undefined
-              }
+              style={iconStyle}
             />
             <Popconfirm
               title={
@@ -260,7 +250,7 @@ export function AgentTable({
                     ? t("agent.defaultNotDisablable")
                     : startupInProgress
                     ? t("agent.status.waitUntilStarted")
-                    : secondaryActionsDisabled
+                    : !capabilities.toggle
                     ? t("agent.secondaryAgentUnsupported")
                     : undefined
                 }
@@ -286,7 +276,7 @@ export function AgentTable({
                     ? t("agent.defaultNotDeletable")
                     : startupInProgress
                     ? t("agent.status.waitUntilStarted")
-                    : secondaryActionsDisabled
+                    : !capabilities.delete
                     ? t("agent.secondaryAgentUnsupported")
                     : undefined
                 }
diff --git a/src/pages/Settings/Agents/index.tsx b/src/pages/Settings/Agents/index.tsx
index a030400..588bc4b 100644
--- a/src/pages/Settings/Agents/index.tsx
+++ b/src/pages/Settings/Agents/index.tsx
@@ -16,8 +16,6 @@ import {
 } from "./profileForm";
 import styles from "./index.module.less";

-const SECONDARY_AGENT_CAPABILITY = "unsupported";
-
 export default function AgentsPage() {
   const { t } = useTranslation();
   const {
@@ -29,14 +27,12 @@ export default function AgentsPage() {
     loadAgents,
     setAgents,
   } = useAgents();
-  const { selectedAgent, setSelectedAgent } = useAgentStore();
+  const { selectedAgent, capabilities, setSelectedAgent } = useAgentStore();
   const [modalVisible, setModalVisible] = useState(false);
   const [editingAgent, setEditingAgent] = useState<AgentSummary | null>(null);
   const [reordering, setReordering] = useState(false);
   const [form] = Form.useForm();
   const { message } = useAppMessage();
-  const secondaryAgentActionsDisabled =
-    SECONDARY_AGENT_CAPABILITY === "unsupported";

   const handleEdit = async (agent: AgentSummary) => {
     try {
@@ -111,6 +107,9 @@ export default function AgentsPage() {
   };

   const handleReorder = async (activeId: string, overId: string) => {
+    if (!capabilities.reorder) {
+      return;
+    }
     const nextAgents = reorderAgents(agents, activeId, overId);
     if (nextAgents === agents) {
       return;
@@ -139,12 +138,18 @@ export default function AgentsPage() {
         current={t("agent.agents")}
         extra={
           <div className={styles.headerRight}>
-            <Tooltip title={t("agent.secondaryAgentUnsupported")}>
+            <Tooltip
+              title={
+                capabilities.create
+                  ? undefined
+                  : t("agent.secondaryAgentUnsupported")
+              }
+            >
               <span>
                 <Button
                   type="primary"
                   icon={<PlusOutlined />}
-                  disabled={secondaryAgentActionsDisabled}
+                  disabled={!capabilities.create}
                 >
                   {t("agent.create")}
                 </Button>
@@ -171,7 +176,7 @@ export default function AgentsPage() {
           onToggle={handleToggle}
           onPin={handlePin}
           onReorder={handleReorder}
-          secondaryAgentActionsDisabled={secondaryAgentActionsDisabled}
+          capabilities={capabilities}
         />
       </Card>

diff --git a/src/stores/agentStore.test.ts b/src/stores/agentStore.test.ts
index 331cf37..e3a19b2 100644
--- a/src/stores/agentStore.test.ts
+++ b/src/stores/agentStore.test.ts
@@ -2,7 +2,7 @@ import { describe, it, expect, beforeEach, vi } from "vitest";
 import { useAgentStore } from "./agentStore";
 import type { AgentSummary } from "@/api/types/agents";
 import { buildAuthHeaders } from "@/api/authHeaders";
-import { clearValidatedDefaultAgent } from "@/api/agentScope";
+import { clearValidatedAgents } from "@/api/agentScope";

 const mocks = vi.hoisted(() => ({
   listAgents: vi.fn(),
@@ -21,7 +21,7 @@ const DEFAULT_AGENT_ID = "10000000-0000-4000-8000-000000000011";
 describe("agentStore", () => {
   beforeEach(() => {
     vi.clearAllMocks();
-    clearValidatedDefaultAgent();
+    clearValidatedAgents();
     sessionStorage.clear();
     localStorage.clear();
     // Reset to initial state before each test
@@ -142,6 +142,72 @@ describe("agentStore", () => {
     });
   });

+  it("keeps a listed non-default selection across refresh", async () => {
+    const secondaryId = "10000000-0000-4000-8000-000000000012";
+    useAgentStore.setState({ selectedAgent: secondaryId, agents: [] });
+    const defaultAgent = {
+      ...mockAgent(DEFAULT_AGENT_ID),
+      is_default: true,
+      enabled: true,
+      pinned: true,
+    };
+    const secondaryAgent = {
+      ...mockAgent(secondaryId),
+      is_default: false,
+      enabled: true,
+      pinned: true,
+    };
+    mocks.listAgents.mockResolvedValue({
+      agents: [defaultAgent, secondaryAgent],
+    });
+
+    await useAgentStore.getState().refreshAgents();
+
+    expect(useAgentStore.getState().selectedAgent).toBe(secondaryId);
+    expect(buildAuthHeaders()).toEqual({
+      "X-Agent-Id": secondaryId,
+      "x-digitalmate-agent-id": secondaryId,
+    });
+  });
+
+  it("stores the capabilities reported by the agent list", async () => {
+    mocks.listAgents.mockResolvedValue({
+      agents: [],
+      capabilities: {
+        multi_agent: true,
+        create: false,
+        import: false,
+        clone: false,
+        delete: false,
+        toggle: false,
+        pin: false,
+        reorder: false,
+      },
+    });
+
+    await useAgentStore.getState().refreshAgents();
+
+    expect(useAgentStore.getState().capabilities.multi_agent).toBe(true);
+    expect(useAgentStore.getState().capabilities.delete).toBe(false);
+  });
+
+  it("treats a missing capabilities payload as all disabled", async () => {
+    mocks.listAgents.mockResolvedValue({ agents: [] });
+
+    await useAgentStore.getState().refreshAgents();
+
+    expect(useAgentStore.getState().capabilities).toEqual({
+      multi_agent: false,
+      create: false,
+      import: false,
+      clone: false,
+      delete: false,
+      toggle: false,
+      pin: false,
+      reorder: false,
+    });
+  });
+
   it("allows a new refresh after the previous request settles", async () => {
     mocks.listAgents.mockResolvedValue({ agents: [] });

diff --git a/src/stores/agentStore.ts b/src/stores/agentStore.ts
index 92624eb..d0f9318 100644
--- a/src/stores/agentStore.ts
+++ b/src/stores/agentStore.ts
@@ -1,10 +1,14 @@
 import { create } from "zustand";
 import { persist } from "zustand/middleware";
-import type { AgentSummary } from "../api/types/agents";
+import {
+  DISABLED_AGENT_CAPABILITIES,
+  type AgentCapabilities,
+  type AgentSummary,
+} from "../api/types/agents";
 import { agentsApi } from "../api/modules/agents";
 import {
-  clearValidatedDefaultAgent,
-  markValidatedDefaultAgent,
+  clearValidatedAgents,
+  markValidatedAgents,
 } from "../api/agentScope";
 import { menuRegistry } from "../plugins/registry/store";

@@ -25,6 +29,7 @@ let agentRefreshPromise: Promise<void> | null = null;
 interface AgentStore {
   selectedAgent: string;
   agents: AgentSummary[];
+  capabilities: AgentCapabilities;
   /** Per-agent last active chat ID for restoring on agent switch */
   lastChatIdByAgent: Record<string, string>;
   setSelectedAgent: (agentId: string) => void;
@@ -83,18 +88,11 @@ export const useAgentStore = create<AgentStore>()(
     (set, get) => ({
       selectedAgent: getInitialSelectedAgent(),
       agents: [],
+      capabilities: { ...DISABLED_AGENT_CAPABILITIES },
       lastChatIdByAgent: {},

       setSelectedAgent: (agentId) => {
         set({ selectedAgent: agentId });
-        const selected = get().agents.find(
-          (agent) => agent.id === agentId && agent.is_default === true,
-        );
-        if (selected) {
-          markValidatedDefaultAgent(selected.id);
-        } else {
-          clearValidatedDefaultAgent();
-        }
         menuRegistry.refresh();
         // Persist to localStorage so new tabs inherit this choice
         try {
@@ -115,21 +113,36 @@ export const useAgentStore = create<AgentStore>()(
           const defaultAgent = response.agents.find(
             (agent) => agent.is_default === true,
           );
-          if (defaultAgent) {
-            markValidatedDefaultAgent(defaultAgent.id);
-            set({
-              agents: response.agents,
-              selectedAgent: defaultAgent.id,
-            });
+          if (response.agents.length === 0) {
+            clearValidatedAgents();
+          } else {
+            markValidatedAgents(
+              response.agents.map((agent) => agent.id),
+              defaultAgent?.id ?? null,
+            );
+          }
+          const previousSelected = get().selectedAgent;
+          const selectionIsUsable = response.agents.some(
+            (agent) => agent.id === previousSelected && agent.enabled,
+          );
+          const nextSelected = selectionIsUsable
+            ? previousSelected
+            : defaultAgent?.id ?? previousSelected;
+          set({
+            agents: response.agents,
+            capabilities: response.capabilities ?? {
+              ...DISABLED_AGENT_CAPABILITIES,
+            },
+            selectedAgent: nextSelected,
+          });
+          if (nextSelected !== previousSelected) {
+            menuRegistry.refresh();
             try {
-              localStorage.setItem(LAST_USED_AGENT_KEY, defaultAgent.id);
+              localStorage.setItem(LAST_USED_AGENT_KEY, nextSelected);
             } catch {
               /* ignore */
             }
-            return;
           }
-          clearValidatedDefaultAgent();
-          set({ agents: response.agents });
         });
         try {
           await agentRefreshPromise;
